import { Input, TBPlugin, UIDialog, I18n, FileUploader } from '@textbus/core';
export declare class PasteUploadEmitterPlugin implements TBPlugin {
    private input;
    private i18n;
    private fileUploader;
    private dialog;
    constructor(input: Input, i18n: I18n, fileUploader: FileUploader, dialog: UIDialog);
    setup(): void;
}
