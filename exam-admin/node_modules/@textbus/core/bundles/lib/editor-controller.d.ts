import { Observable } from 'rxjs';
export interface GlobalStatus {
    readonly: boolean;
    sourcecodeMode: boolean;
    supportMarkdown: boolean;
}
export declare class EditorController {
    private status;
    onStateChange: Observable<GlobalStatus>;
    set supportMarkdown(b: boolean);
    get supportMarkdown(): boolean;
    set readonly(b: boolean);
    get readonly(): boolean;
    set sourceCodeMode(b: boolean);
    get sourceCodeMode(): boolean;
    private stateChangeEvent;
    constructor(status: GlobalStatus);
    private dispatch;
}
