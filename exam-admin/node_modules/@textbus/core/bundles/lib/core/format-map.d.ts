import { BlockFormatter, FormatRange, InlineFormatter } from './formatter';
/**
 * 内部是对 Map<InlineFormatter | BlockFormatter, FormatRange[]> 的封装。
 * 并提供格式合并的功能。
 */
export declare class FormatMap {
    private map;
    keys(): IterableIterator<InlineFormatter | BlockFormatter>;
    get(formatter: InlineFormatter | BlockFormatter): FormatRange[];
    set(formatter: InlineFormatter | BlockFormatter, formatRanges: FormatRange[]): void;
    /**
     * 合并格式
     * @param token 当前格式类别
     * @param formatter 当前要合并的格式
     * @param important 合并的优先级
     */
    merge(token: InlineFormatter | BlockFormatter, formatter: FormatRange, important: boolean): void;
}
