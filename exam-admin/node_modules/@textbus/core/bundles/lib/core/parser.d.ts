import { Fragment } from './fragment';
import { Formatter } from './formatter';
import { ComponentLoader } from './component';
/**
 * Parser 类用于把一段 DOM 转换为组件（Component）和可编辑片段（Fragment）的抽象数据树
 */
export declare class Parser {
    private componentLoaders;
    private formatters;
    static parseHTML(html: string): HTMLElement;
    /** @deprecated */
    static parserHTML(html: string): HTMLElement;
    constructor(componentLoaders?: ComponentLoader[], formatters?: Formatter[]);
    parse(el: HTMLElement): Fragment;
    private readComponent;
    private readFormats;
}
