/**
 * 抽象数据字面量
 */
export interface FormatDataParams {
    tag?: string;
    classes?: string[];
    attrs?: {
        [key: string]: string | number | boolean;
    } | Map<string, string | number | boolean>;
    styles?: {
        [key: string]: string | number;
    } | Map<string, string | number>;
}
/**
 * 抽象数据类，用于记录一个节点或样式的摘要数据
 */
export declare class FormatData {
    readonly tag: string;
    readonly attrs: Map<string, string | number | boolean>;
    readonly styles: Map<string, string | number>;
    readonly classes: string[];
    constructor(params?: FormatDataParams);
    /**
     * 复制当前抽象数据的副本。
     */
    clone(): FormatData;
    /**
     * 判断 data 是否和自己相等。
     * @param data
     */
    equal(data: FormatData): boolean;
    private static equalStringList;
    private static equalMap;
}
