export interface TabConfig {
    label: string;
    view: HTMLElement;
}
export declare class Tab {
    elementRef: HTMLElement;
    head: HTMLElement;
    private btnWrapper;
    private viewContainer;
    constructor();
    show(config: TabConfig[]): void;
}
