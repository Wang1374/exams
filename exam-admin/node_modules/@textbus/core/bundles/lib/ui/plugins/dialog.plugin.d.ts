import { TBPlugin } from '../../plugin';
import { Layout } from '../layout';
import { EditorController } from '../../editor-controller';
export declare class UIDialog implements TBPlugin {
    private layout;
    private editorController;
    private elementRef;
    private dialogWrapper;
    private timer;
    private subs;
    constructor(layout: Layout, editorController: EditorController);
    setup(): void;
    dialog(element: HTMLElement): void;
    close(): void;
    onDestroy(): void;
}
