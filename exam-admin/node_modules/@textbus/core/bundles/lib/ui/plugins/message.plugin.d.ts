import { TBPlugin } from '../../plugin';
import { Layout } from '../layout';
export declare class UIMessage implements TBPlugin {
    private layout;
    private messageBox;
    constructor(layout: Layout);
    setup(): void;
    message(message: string, time?: number): void;
    info(message: string, time?: number): void;
    success(message: string, time?: number): void;
    warning(message: string, time?: number): void;
    danger(message: string, time?: number): void;
    private createMessage;
}
