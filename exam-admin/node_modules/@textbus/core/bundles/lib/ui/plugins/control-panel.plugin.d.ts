import { ComponentControlPanelView } from '../../core/_api';
import { EditorController } from '../../editor-controller';
import { TBPlugin } from '../../plugin';
import { Layout } from '../layout';
import { I18n } from '../../i18n/i18n';
export declare class UIControlPanel implements TBPlugin {
    private editorController;
    private i18n;
    private layout;
    elementRef: HTMLElement;
    private container;
    private fixedBtn;
    private set fixed(value);
    private get fixed();
    private _fixed;
    private subs;
    private oldViews;
    private tab;
    constructor(editorController: EditorController, i18n: I18n, layout: Layout);
    setup(): void;
    showPanels(views: ComponentControlPanelView[]): void;
    onDestroy(): void;
}
