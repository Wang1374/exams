import { VElement } from './core/_api';
export declare abstract class OutputTranslator {
    abstract transform(vDom: VElement): any;
}
/**
 * HTML 输出转换器
 */
export declare class HTMLOutputTranslator implements OutputTranslator {
    static singleTags: string[];
    static simpleXSSFilter: {
        text(text: string): string;
        attrName(text: string): any;
        attrValue(text: string): any;
        symbolTransfer(text: string): string;
    };
    private singleTagTest;
    transform(vDom: VElement): string;
    private vDomToHTMLString;
}
