import { Injector, Type } from '@tanbo/di';
import { AbstractComponent } from './core/_api';
/**
 * 组件 Injector 缓存器
 */
export declare class ComponentInjectors {
    private injectors;
    set(componentConstruct: Type<AbstractComponent>, injector: Injector): void;
    get(componentConstruct: Type<AbstractComponent>): Injector;
    destroy(): void;
}
