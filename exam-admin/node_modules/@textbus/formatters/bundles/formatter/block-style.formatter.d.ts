import { MatchRule, FormatEffect, BlockFormatter, FormatData, VElement, FormatRendingContext } from '@textbus/core';
export declare class BlockStyleFormatter extends BlockFormatter {
    styleName: string | string[];
    constructor(styleName: string | string[], rule: MatchRule);
    read(node: HTMLElement): FormatData;
    render(context: FormatRendingContext, existingElement?: VElement): VElement;
}
export declare const textIndentFormatter: BlockStyleFormatter;
export declare const textAlignFormatter: BlockStyleFormatter;
export declare class BlockBackgroundColorFormatter extends BlockStyleFormatter {
    static blockTags: string[];
    read(node: HTMLElement): FormatData;
    match(p: HTMLElement): FormatEffect;
}
export declare const blockBackgroundColorFormatter: BlockBackgroundColorFormatter;
