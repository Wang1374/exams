import { BlockFormatter, FormatData, FormatEffect, FormatRendingContext, InlineFormatter, VElement } from '@textbus/core';
export declare class InlinePaddingFormatter extends InlineFormatter {
    static inlineTags: string[];
    reg: RegExp;
    constructor();
    match(p: HTMLElement): FormatEffect;
    read(node: HTMLElement): FormatData;
    render(context: FormatRendingContext, existingElement?: VElement): VElement;
}
export declare class BlockPaddingFormatter extends BlockFormatter {
    static blockTags: string[];
    reg: RegExp;
    constructor();
    match(p: HTMLElement): FormatEffect;
    read(node: HTMLElement): FormatData;
    render(context: FormatRendingContext, existingElement?: VElement): VElement;
}
export declare const inlinePaddingFormatter: InlinePaddingFormatter;
export declare const blockPaddingFormatter: BlockPaddingFormatter;
