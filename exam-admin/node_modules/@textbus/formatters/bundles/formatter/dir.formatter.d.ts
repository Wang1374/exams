import { BlockFormatter, FormatData, FormatRendingContext, VElement } from '@textbus/core';
export declare class DirFormatter extends BlockFormatter {
    constructor();
    read(node: HTMLElement): FormatData;
    render(context: FormatRendingContext, existingElement?: VElement): VElement;
}
export declare const dirFormatter: DirFormatter;
