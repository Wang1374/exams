import { BlockFormatter, FormatData, FormatEffect, FormatRendingContext, InlineFormatter, VElement } from '@textbus/core';
export declare class InlineMarginFormatter extends InlineFormatter {
    static inlineTags: string[];
    reg: RegExp;
    constructor();
    match(p: HTMLElement): FormatEffect;
    read(node: HTMLElement): FormatData;
    render(context: FormatRendingContext, existingElement?: VElement): VElement;
}
export declare class BlockMarginFormatter extends BlockFormatter {
    static blockTags: string[];
    reg: RegExp;
    constructor();
    match(p: HTMLElement): FormatEffect;
    read(node: HTMLElement): FormatData;
    render(context: FormatRendingContext, existingElement?: VElement): VElement;
}
export declare const inlineMarginFormatter: InlineMarginFormatter;
export declare const blockMarginFormatter: BlockMarginFormatter;
