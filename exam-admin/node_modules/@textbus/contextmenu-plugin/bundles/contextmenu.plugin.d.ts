import { TBPlugin, Input, I18n, RootComponent, TBSelection } from '@textbus/core';
export declare class ContextmenuPlugin implements TBPlugin {
    private input;
    private i18n;
    private selection;
    private rootComponent;
    constructor(input: Input, i18n: I18n, selection: TBSelection, rootComponent: RootComponent);
    setup(): void;
    private insertParagraph;
}
