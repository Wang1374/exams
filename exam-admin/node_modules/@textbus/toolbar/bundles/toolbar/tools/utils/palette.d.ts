import { FormatData } from '@textbus/core';
import { Observable } from 'rxjs';
import { DropdownViewer } from '../../toolkit/dropdown-tool';
export declare class Palette implements DropdownViewer {
    private styleName;
    static defaultColors: string[];
    elementRef: HTMLDivElement;
    onComplete: Observable<string>;
    private picker;
    private completeEvent;
    constructor(styleName: string, btnText: string, colors?: string[]);
    update(d?: FormatData): void;
}
