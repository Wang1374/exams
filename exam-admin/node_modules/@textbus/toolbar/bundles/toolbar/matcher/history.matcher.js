import { TBHistory } from '@textbus/core';
import { HighlightState } from '../help';
export class HistoryMatcher {
    constructor(type) {
        this.type = type;
    }
    setup(injector) {
        this.history = injector.get(TBHistory);
    }
    queryState() {
        switch (this.type) {
            case 'back':
                return {
                    state: this.history.canBack ? HighlightState.Normal : HighlightState.Disabled,
                    srcStates: [],
                    matchData: null
                };
            case 'forward':
                return {
                    state: this.history.canForward ? HighlightState.Normal : HighlightState.Disabled,
                    srcStates: [],
                    matchData: null
                };
        }
    }
}
//# sourceMappingURL=history.matcher.js.map