import { Type, TBSelection, BranchAbstractComponent, DivisionAbstractComponent, BackboneAbstractComponent } from '@textbus/core';
import { BlockComponent } from '@textbus/components';
import { Matcher, SelectionMatchState } from './matcher';
export declare class BlockMatcher implements Matcher {
    componentConstructor: Type<BlockComponent>;
    private tagNames;
    private excludeComponents;
    constructor(componentConstructor: Type<BlockComponent>, tagNames: string[], excludeComponents?: Array<Type<BranchAbstractComponent | BackboneAbstractComponent | DivisionAbstractComponent>>);
    queryState(selection: TBSelection): SelectionMatchState;
}
