import { Injector } from '@textbus/core';
import { Commander } from '../commander';
export declare class HistoryCommander implements Commander<null> {
    private action;
    recordHistory: boolean;
    private history;
    constructor(action: 'forward' | 'back');
    setup(injector: Injector): void;
    command(): void;
}
