import { Type, BlockFormatter, InlineFormatter } from '@textbus/core';
import { CommandContext, Commander } from '../commander';
export declare class CleanCommander implements Commander<null> {
    private excludeFormatters;
    recordHistory: boolean;
    constructor(excludeFormatters?: Type<BlockFormatter | InlineFormatter>[]);
    command(context: CommandContext): void;
}
