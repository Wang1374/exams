import { TBHistory } from '@textbus/core';
export class HistoryCommander {
    constructor(action) {
        this.action = action;
        this.recordHistory = false;
    }
    setup(injector) {
        this.history = injector.get(TBHistory);
    }
    command() {
        this.action === 'back' ?
            this.history.back() :
            this.history.forward();
    }
}
//# sourceMappingURL=history.commander.js.map