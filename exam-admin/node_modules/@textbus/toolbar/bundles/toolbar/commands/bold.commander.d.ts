import { InlineFormatter } from '@textbus/core';
import { CommandContext, Commander } from '../commander';
export declare class BoldCommander implements Commander<null> {
    private formatter;
    recordHistory: boolean;
    constructor(formatter: InlineFormatter);
    command(context: CommandContext): void;
}
