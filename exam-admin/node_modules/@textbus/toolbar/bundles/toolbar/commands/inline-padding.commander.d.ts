import { InlinePaddingFormatter } from '@textbus/formatters';
import { CommandContext, Commander } from '../commander';
export declare class InlinePaddingCommander implements Commander<Map<string, string>> {
    private inlineFormatter;
    recordHistory: boolean;
    constructor(inlineFormatter: InlinePaddingFormatter);
    command(context: CommandContext, params: Map<string, string>): void;
}
