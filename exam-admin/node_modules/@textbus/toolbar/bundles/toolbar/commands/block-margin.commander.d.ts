import { BlockMarginFormatter } from '@textbus/formatters';
import { CommandContext, Commander } from '../commander';
export declare class BlockMarginCommander implements Commander<Map<string, string>> {
    private formatter;
    recordHistory: boolean;
    constructor(formatter: BlockMarginFormatter);
    command(context: CommandContext, params: Map<string, string>): void;
    private apply;
}
