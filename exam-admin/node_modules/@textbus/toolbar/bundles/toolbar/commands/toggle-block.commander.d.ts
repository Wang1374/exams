import { CommandContext, Commander } from '../commander';
export declare class ToggleBlockCommander implements Commander<null> {
    private tagName;
    recordHistory: boolean;
    constructor(tagName: string);
    command(context: CommandContext): void;
}
