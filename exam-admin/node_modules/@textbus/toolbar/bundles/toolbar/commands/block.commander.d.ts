import { CommandContext, Commander } from '../commander';
export declare class BlockCommander implements Commander<string> {
    recordHistory: boolean;
    private tagName;
    command(context: CommandContext, tagName: string): void;
    private effect;
}
