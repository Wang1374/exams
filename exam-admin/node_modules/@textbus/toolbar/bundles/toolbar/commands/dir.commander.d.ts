import { DirFormatter } from '@textbus/formatters';
import { CommandContext, Commander } from '../commander';
export declare class DirCommander implements Commander<null> {
    private formatter;
    private dir;
    recordHistory: boolean;
    constructor(formatter: DirFormatter, dir: 'ltr' | 'rtl');
    command(context: CommandContext): void;
}
