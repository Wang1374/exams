import { InlineMarginFormatter } from '@textbus/formatters';
import { CommandContext, Commander } from '../commander';
export declare class InlineMarginCommander implements Commander<Map<string, string>> {
    private inlineFormatter;
    recordHistory: boolean;
    constructor(inlineFormatter: InlineMarginFormatter);
    command(context: CommandContext, params: Map<string, string>): void;
}
