import { BlockPaddingFormatter } from '@textbus/formatters';
import { CommandContext, Commander } from '../commander';
export declare class BlockPaddingCommander implements Commander<Map<string, string>> {
    private formatter;
    recordHistory: boolean;
    constructor(formatter: BlockPaddingFormatter);
    command(context: CommandContext, params: Map<string, string>): void;
    private apply;
}
