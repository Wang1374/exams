import { CommandContext, Commander } from '../commander';
export declare class ListCommander implements Commander<null> {
    private tagName;
    recordHistory: boolean;
    constructor(tagName: 'ol' | 'ul');
    command(context: CommandContext): void;
    private buildNewList;
    private getMovableContents;
}
