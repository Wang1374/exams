import { VerticalAlignFormatter } from '@textbus/formatters';
import { CommandContext, Commander } from '../commander';
export declare class VerticalAlignCommander implements Commander<string> {
    private formatter;
    recordHistory: boolean;
    constructor(formatter: VerticalAlignFormatter);
    command(context: CommandContext, params: string): void;
}
