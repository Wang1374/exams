import { CommandContext, Commander } from '../commander';
export declare class ImageCommander implements Commander<Map<string, any>> {
    recordHistory: boolean;
    command(context: CommandContext, attrs: Map<string, any>): void;
}
