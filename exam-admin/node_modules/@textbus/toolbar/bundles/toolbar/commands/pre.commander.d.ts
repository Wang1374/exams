import { CommandContext, Commander } from '../commander';
export declare class PreCommander implements Commander<string> {
    recordHistory: boolean;
    command(context: CommandContext, lang: string): void;
}
