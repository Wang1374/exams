import { LeafAbstractComponent, VElement } from '@textbus/core';
export declare class AudioComponent extends LeafAbstractComponent {
    src: string;
    autoplay: boolean;
    controls: boolean;
    block: boolean;
    constructor(src: string, autoplay: boolean, controls: boolean);
    render(): VElement;
    clone(): AudioComponent;
}
