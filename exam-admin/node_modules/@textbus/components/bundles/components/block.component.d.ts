import { DivisionAbstractComponent, Formatter, Fragment, SingleSlotRenderFn, SlotRenderFn, VElement } from '@textbus/core';
export declare class BlockComponent extends DivisionAbstractComponent {
    static blockTags: string[];
    static cleanedFormatters: Formatter[];
    static breakingLine(fragment: Fragment, index: number): Fragment;
    constructor(tagName: string);
    clone(): BlockComponent;
    slotRender(isOutputMode: boolean, slotRendererFn: SingleSlotRenderFn): VElement;
    render(isOutputMode: boolean, slotRendererFn: SlotRenderFn): VElement;
}
