import { BranchAbstractComponent, Fragment, VElement, SlotRenderFn, SingleSlotRenderFn } from '@textbus/core';
export declare class ListComponent extends BranchAbstractComponent {
    constructor(tagName: string);
    clone(): ListComponent;
    slotRender(slot: Fragment, isOutputMode: boolean, slotRendererFn: SingleSlotRenderFn): VElement;
    render(isOutputMode: boolean, slotRendererFn: SlotRenderFn): VElement;
    split(startIndex: number, endIndex: number): {
        before: Fragment[];
        center: Fragment[];
        after: Fragment[];
    };
}
