import { LeafAbstractComponent, VElement } from '@textbus/core';
export declare class VideoComponent extends LeafAbstractComponent {
    src: string;
    autoplay: boolean;
    controls: boolean;
    block: boolean;
    width: string;
    height: string;
    constructor(src: string, autoplay: boolean, controls: boolean, option?: {
        width: string;
        height: string;
    });
    render(): VElement;
    clone(): VideoComponent;
}
