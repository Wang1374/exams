import { TBPlugin, RootComponent, TBSelection, Renderer } from '@textbus/core';
export declare class GuardEndBlockPlugin implements TBPlugin {
    private rootComponent;
    private renderer;
    private selection;
    private subs;
    constructor(rootComponent: RootComponent, renderer: Renderer, selection: TBSelection);
    setup(): void;
    onDestroy(): void;
    private guardLastIsParagraph;
}
