import { Observable } from 'rxjs';
import { FormatData, BranchAbstractComponent, LeafAbstractComponent, FileUploader } from '@textbus/core';
import { FormItem } from './help';
export interface FormConfig {
    title?: string;
    editProperty?: 'attrs' | 'styles';
    items: Array<FormItem>;
    mini?: boolean;
    maxHeight?: string;
    confirmBtnText?: string;
    cancelBtnText?: string;
}
export declare class Form {
    private config;
    onComplete: Observable<Map<string, any>>;
    onClose: Observable<void>;
    body: HTMLElement;
    footer: HTMLElement;
    readonly elementRef: HTMLFormElement;
    private completeEvent;
    private closeEvent;
    constructor(config: FormConfig);
    addItem(item: FormItem, index?: number): void;
    removeItem(item: FormItem): void;
    reset(): void;
    setFileUploader(fileUploader: FileUploader): void;
    update(d: FormatData | BranchAbstractComponent | LeafAbstractComponent): void;
}
