import { FileUploader } from '@textbus/core';
import { AttrState, FormTextFieldParams, FormItem } from './help';
export declare class FormTextField implements FormItem<string> {
    private config;
    elementRef: HTMLDivElement;
    name: string;
    private input;
    private sub;
    private readonly btn;
    private readonly feedbackEle;
    private uploader;
    constructor(config: FormTextFieldParams);
    useUploader(uploader: FileUploader): void;
    reset(): void;
    update(value?: any): void;
    getAttr(): AttrState<string>;
    validate(): boolean;
    private uploaded;
}
